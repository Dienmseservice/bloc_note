{
  "version": 2,
  "framework": null,
  "buildCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && ./flutter/bin/flutter pub get && ./flutter/bin/flutter pub run sqflite_common_ffi_web:setup && ./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "regions": ["cdg1"],
  "trailingSlash": false,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}